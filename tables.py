# This dict actually has tables from multiple schemas,
# but the sample data can be loaded into a single schema.

Tables = {
    "public": [
        "test_sample_a",
        "test_sample_b",
        "test_sample_c"
    ]
}